{
  "name": "12_installpkg_npm",
  "version": "1.0.0",
  "description": "tutorial",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Martin walsh",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.11" //major.minor.patch version
                        // "^" means if minor or patch version update then install but not if a major update (4.x.x)
                        // "~" only give patch updates (4.17.x)
                        // "no symbol" means to never update to next version
  }
}
